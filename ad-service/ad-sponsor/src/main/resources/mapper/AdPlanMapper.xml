<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cx.ad.dao.AdPlanMapper">

    <resultMap type="com.cx.ad.entity.AdPlan" id="AdPlanMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="userId" column="user_id" jdbcType="INTEGER"/>
        <result property="planName" column="plan_name" jdbcType="VARCHAR"/>
        <result property="planStatus" column="plan_status" jdbcType="OTHER"/>
        <result property="startDate" column="start_date" jdbcType="TIMESTAMP"/>
        <result property="endDate" column="end_date" jdbcType="TIMESTAMP"/>
        <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
        <result property="updateTime" column="update_time" jdbcType="TIMESTAMP"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="AdPlanMap">
        select
          id, user_id, plan_name, plan_status, start_date, end_date, create_time, update_time
        from ad-data.ad_plan
        where id = #{id}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="AdPlanMap">
        select
          id, user_id, plan_name, plan_status, start_date, end_date, create_time, update_time
        from ad-data.ad_plan
        limit #{offset}, #{limit}
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAll" resultMap="AdPlanMap">
        select
          id, user_id, plan_name, plan_status, start_date, end_date, create_time, update_time
        from ad-data.ad_plan
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="userId != null">
                and user_id = #{userId}
            </if>
            <if test="planName != null and planName != ''">
                and plan_name = #{planName}
            </if>
            <if test="planStatus != null">
                and plan_status = #{planStatus}
            </if>
            <if test="startDate != null">
                and start_date = #{startDate}
            </if>
            <if test="endDate != null">
                and end_date = #{endDate}
            </if>
            <if test="createTime != null">
                and create_time = #{createTime}
            </if>
            <if test="updateTime != null">
                and update_time = #{updateTime}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into ad-data.ad_plan(user_id, plan_name, plan_status, start_date, end_date, create_time, update_time)
        values (#{userId}, #{planName}, #{planStatus}, #{startDate}, #{endDate}, #{createTime}, #{updateTime})
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update ad-data.ad_plan
        <set>
            <if test="userId != null">
                user_id = #{userId},
            </if>
            <if test="planName != null and planName != ''">
                plan_name = #{planName},
            </if>
            <if test="planStatus != null">
                plan_status = #{planStatus},
            </if>
            <if test="startDate != null">
                start_date = #{startDate},
            </if>
            <if test="endDate != null">
                end_date = #{endDate},
            </if>
            <if test="createTime != null">
                create_time = #{createTime},
            </if>
            <if test="updateTime != null">
                update_time = #{updateTime},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from ad-data.ad_plan where id = #{id}
    </delete>

</mapper>